# YardageBook — Incremento 5.5: Dominio de campos (puro)

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development / executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** Añadir al dominio puro los tipos y la geometría que necesita el flujo de Juego: `LatLng`, `GreenInfo`/`Hole`/`Course` y `haversineMeters`. TypeScript puro, sin React ni servicios, 100% testeado. No toca UI ni rutas.

**Architecture:** Capa `src/domain`. Modelos en `models/`, geometría en `geo/`. `domain/` no importa de `services/` ni `ui/` (regla de oro). Estos tipos los consumirán después el servicio `courses` (Inc.6) y la pantalla de Hoyo (Inc.7).

**Ref:** [Proveedor de campos OSM](../specs/2026-06-05-courses-provider-osm.md) §2 (modelos) y §6 (distancia).

---

## Estructura de ficheros

```
src/domain/
├── models/
│   ├── geo.ts            ← LatLng
│   ├── course.ts         ← GreenInfo, Hole, Course
│   └── index.ts          ← (modify) re-exporta geo + course
├── geo/
│   ├── distance.ts       ← haversineMeters()
│   ├── distance.test.ts
│   └── index.ts          ← barrel
└── index.ts              ← (modify) re-exporta geo
```

---

## Task 1: Tipos de dominio (geo + course)

**Files:** `src/domain/models/geo.ts`, `src/domain/models/course.ts`, `src/domain/models/index.ts` (modify)

- [ ] **Step 1: `src/domain/models/geo.ts`**
```ts
/** Coordenada geográfica (WGS84). */
export interface LatLng {
  lat: number;
  lng: number;
}
```

- [ ] **Step 2: `src/domain/models/course.ts`**
```ts
import { LatLng } from './geo';

/** Información del green de un hoyo. */
export interface GreenInfo {
  /** Centro del green (centroide del polígono OSM golf=green). MVP usa esto. */
  center: LatLng;
  /** Contorno del green, para dibujarlo en el mapa. Opcional. */
  polygon?: LatLng[];
}

/** Un hoyo del campo. */
export interface Hole {
  /** Número de hoyo, 1..18 (o 1..9). */
  ref: number;
  /** Par del hoyo, si OSM lo trae. */
  par?: number;
  /** Stroke index / hándicap del hoyo (S.I.), si OSM lo trae. */
  strokeIndex?: number;
  /** Green del hoyo (siempre presente si el hoyo se incluye). */
  green: GreenInfo;
  /** Puntos de tee del hoyo (varios). Para encuadrar el mapa. */
  tees: LatLng[];
  /** Línea de juego tee→green. Para encuadre y dibujo. */
  playLine: LatLng[];
}

/** Un campo de golf normalizado. */
export interface Course {
  /** Id estable, ej. "osm-way-237391513". */
  id: string;
  /** Nombre del campo. */
  name: string;
  /** Origen del dato. */
  source: 'osm';
  /** Centro del campo (listado, búsqueda y encuadre inicial). */
  location: LatLng;
  /** Nº de hoyos con datos. */
  holeCount: number;
  holes: Hole[];
  /** Texto de atribución obligatorio (ODbL). */
  attribution: string;
}
```

- [ ] **Step 3: re-exportar en `src/domain/models/index.ts`** — añadir al final:
```ts
export * from './geo';
export * from './course';
```

- [ ] **Step 4: typecheck** → `npm run typecheck` (exit 0)

- [ ] **Step 5: commit** → `feat(domain): add geo and course models`

---

## Task 2: haversineMeters (TDD)

**Files:** `src/domain/geo/distance.ts`, `src/domain/geo/distance.test.ts`, `src/domain/geo/index.ts`, `src/domain/index.ts` (modify)

- [ ] **Step 1: test que falla** — `src/domain/geo/distance.test.ts`
```ts
import { haversineMeters } from './distance';
import { LatLng } from '../models';

const origin: LatLng = { lat: 0, lng: 0 };

describe('haversineMeters', () => {
  it('es 0 entre un punto y sí mismo', () => {
    expect(haversineMeters(origin, origin)).toBe(0);
  });

  it('un grado de latitud en el ecuador ≈ 111.2 km', () => {
    const d = haversineMeters(origin, { lat: 1, lng: 0 });
    expect(d).toBeGreaterThan(111000);
    expect(d).toBeLessThan(111400);
  });

  it('un grado de longitud en el ecuador ≈ 111.2 km', () => {
    const d = haversineMeters(origin, { lat: 0, lng: 1 });
    expect(d).toBeGreaterThan(111000);
    expect(d).toBeLessThan(111400);
  });

  it('es simétrica', () => {
    const a: LatLng = { lat: 36.28, lng: -5.34 };
    const b: LatLng = { lat: 36.29, lng: -5.33 };
    expect(haversineMeters(a, b)).toBeCloseTo(haversineMeters(b, a), 6);
  });
});
```

- [ ] **Step 2: ver fallar** → `npx jest src/domain/geo/distance.test.ts` (módulo no encontrado)

- [ ] **Step 3: implementar `src/domain/geo/distance.ts`**
```ts
import { LatLng } from '../models';

/** Distancia haversine en metros entre dos coordenadas. */
export function haversineMeters(a: LatLng, b: LatLng): number {
  const R = 6371000;
  const toRad = (d: number) => (d * Math.PI) / 180;
  const dLat = toRad(b.lat - a.lat);
  const dLng = toRad(b.lng - a.lng);
  const s =
    Math.sin(dLat / 2) ** 2 +
    Math.cos(toRad(a.lat)) * Math.cos(toRad(b.lat)) * Math.sin(dLng / 2) ** 2;
  return 2 * R * Math.asin(Math.sqrt(s));
}
```

- [ ] **Step 4: ver pasar** → `npx jest src/domain/geo/distance.test.ts` (4 tests)

- [ ] **Step 5: barrel `src/domain/geo/index.ts`**
```ts
export * from './distance';
```

- [ ] **Step 6: re-exportar geo en `src/domain/index.ts`** — añadir:
```ts
export * from './geo';
```

- [ ] **Step 7: verificación** → `npm run typecheck && npm run lint && npm test` (44 tests: 40 previos + 4 distancia)

- [ ] **Step 8: commit** → `feat(domain): add haversine distance`

---

## Definición de "hecho"
- [ ] `LatLng`, `GreenInfo`, `Hole`, `Course`, `haversineMeters` disponibles desde `src/domain`.
- [ ] 44 tests verdes; typecheck y lint limpios.
- [ ] `domain/` sigue sin importar de `services/` ni `ui/`.

## Fuera de alcance
- Servicio `courses` / `BundledCourseProvider` (Inc.6).
- Script batch + datos piloto (Inc.6).
- UI de mapa/GPS (Inc.7).
